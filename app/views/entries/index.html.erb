<nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <%= button_to "<", shift_date_left_path(date: @current_date), class: "nav-link" %>
            </ul>
            <ul class="navbar-nav mx-auto" style="position: absolute; left: 50%; transform: translateX(-50%);">
                <li class="nav-item">
                    <% if @current_date == Date.today %>
                        <%= "Today" %>
                    <% else %>
                        <%= @current_date %>
                    <% end %>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                <%= button_to ">", shift_date_right_path(date: @current_date), class: "nav-link" %>
            </ul>
        </div>
    </div>
</nav>
<div>
 <% @entries.each do |entry| %>
   <div class="container">
      <div class="row">
        <%= entry_form(entry) %>
      </div>
  </div>
   <%= button_to "Delete", entry, method: :delete, data: { confirm: "Are you sure you want to delete this Project ?"} %>
 <% end %>
</div>